# Known OpenAPI page-slug collisions accepted as a baseline (DX-2380).
# New collisions outside this list fail CI. Drive this list down to zero.
#
# Currently empty: the disambiguator (disambiguate_openapi_slugs.py) resolves every
# collision it can find, in every version, by suffixing the losing operation's summary
# ("Create a new user" -> "Create a new user 2"). This file exists as a safety net for
# a collision the disambiguator can't cleanly fix (e.g. no locatable 'summary:' line -
# see the WARNING it prints in that case) so a hard-to-resolve case can be grandfathered
# here instead of blocking every deploy. Add entries as `directory/tag-slug/summary-slug`
# (one per line, from the "cross-spec slug collision(s)" output), with a comment
# explaining why it's accepted.
