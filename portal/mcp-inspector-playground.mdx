---
title: "Test MCP Tools with the Embedded MCP Inspector"
description: "How to use the embedded MCP Inspector to test an MCP product's tools directly from the Developer Portal, before writing agent code."
sidebarTitle: "MCP Inspector Playground"
keywords: "Developer Portal, MCP, Model Context Protocol, MCP Inspector, playground, embedded proxy, JSON-RPC"
---

## Availability

| Component | Version | Editions |
| :--- | :--- | :--- |
| Developer Portal | v1.19.0 | Enterprise |

<Note>
This feature is still under active development. The behavior described on this page may change before release.
</Note>

## Overview

The embedded MCP Inspector lets you test an MCP product's tools directly from the Live Portal, so you can validate a connection and see what a tool returns before writing any agent code.

## Open the Inspector

The Inspector is available for MCP-typed products only.

- From a Product's detail page, select **Open MCP Inspector**.
- The Inspector also loads directly on the MCP documentation page for the API.

The Inspector loads in an embedded frame. It lists the tools the MCP server exposes and lets you call them.

## How the Inspector Connects

The Inspector does not talk to your MCP server directly. It talks to a proxy hosted by the Portal itself, which forwards the connection to the MCP server URL configured on the product. You cannot point the Inspector at a different server; the Portal always decides the upstream.

## Providing Credentials

Unlike the GraphQL Playground, the Inspector does not automatically populate your credentials. Enter your issued credential into the Inspector's connection form yourself. The Portal's proxy forwards it to the MCP server without reading or modifying it.

## Vendored Inspector Version

The Portal ships the official MCP Inspector client, pinned to a specific version. This is not necessarily the latest version available from the MCP Inspector project, so some features documented upstream may not be present yet.

## Troubleshooting

<AccordionGroup>

<Accordion title="The Inspector still connects to an old MCP server URL after an operator changes it">

The proxy caches the upstream address per API. If an operator updates the product's MCP server URL, the change may not take effect until the Portal is restarted.

</Accordion>

<Accordion title="The Inspector shows a connection error immediately">

Check whether the issue is the Portal's proxy or the upstream MCP server itself — the Inspector's banner indicates which side is unreachable.

</Accordion>

</AccordionGroup>
