---
title: "Tyk Operator CRD Reference"
description: "Field reference for every Tyk Operator Custom Resource Definition (CRD), generated from the operator CRD schemas."
keywords: "Tyk Operator, CRD, Custom Resource Definition, Kubernetes, Reference, ApiDefinition, SecurityPolicy"
sidebarTitle: "CRD Reference"
---

import OperatorCRD from '/snippets/operator-crd-reference.mdx';

<Note>
  This page lists the fields of every Tyk Operator Custom Resource under its `spec`. Field names use dot notation for nested objects (`proxy.target_url`), `[]` for list items (`tags[]`), and `.{key}` for map entries (`auth_configs.{key}`). Within each group, required fields are listed first. Read-only `status` fields are not listed.
</Note>

<OperatorCRD />
